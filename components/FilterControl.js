import { useState } from "react";

/**
 * Filter options:
 * - tags
 * - value
 * - currentValue
 * - lifeSpan
 * - purchasedAt
 */

export default function FilterControl({}) {
  return (
    <div>
      
    </div>
  )
}
